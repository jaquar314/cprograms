#include<stdio.h>
int main()
{
    int a[10],b[10],m,n,i,j,c[10];
    printf("\n enter the range of the matrix :\n");
    scanf("%d,%d",&m,&n);
    printf("\n enter the elements");
    for(i=0;i<m;i++)
    {
    }
    printf("ENTER THE SECOND MATRIX:\n");
    {
        scanf("%d%d",&b[i][j]);
    }
    printf("the addition of the matrix is:");
    {
        for(j=0;j<n;j++)
        c[i][j]=a[i][j]+b[i][j];
        for(i=0;i<m;i++)
        {
        for(j=0;j<n;j++)
        printf("%d%d",c[i]c[j])
        return 0;

        }

    }

}